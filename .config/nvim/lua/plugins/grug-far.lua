return {
  {
    "MagicDuck/grug-far.nvim",
    config = function()
      require("grug-far").setup({
        -- Example configuration options (if any exist)
        enable_feature = true, -- Toggle features inspired by Grug
      })
    end,
  },
}
